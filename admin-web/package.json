{
  "name": "ecommerce-cs-agent-admin-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:customer && npm run build:system",
    "build:customer": "vite build --mode customer",
    "build:system": "vite build --mode system",
    "test": "tsc --noEmit && node --test scripts/admin-boundary.test.mjs && node scripts/assert-ui-regressions.mjs && node --test src/mobile-shell.test.mjs",
    "test:landing": "node --test scripts/landing-page-copy.test.mjs"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.1.1",
    "lucide-react": "^0.561.0",
    "react": "^19.2.1",
    "react-dom": "^19.2.1",
    "typescript": "^5.9.3",
    "vite": "^7.2.7"
  },
  "devDependencies": {
    "@types/react": "^19.2.7",
    "@types/react-dom": "^19.2.3"
  },
  "overrides": {
    "esbuild": "0.28.1"
  }
}
